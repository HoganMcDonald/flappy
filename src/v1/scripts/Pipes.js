class Pipes {
  constructor() {
    this.gapY = 80;
    this.gapX = 400;
    this.width = 30;

    this.pipes = [];
  }

  new() {
    pipes.push({x: width, y: 0});
  }

  render() {
    fill('teal');
    rect();
  }

  update() {
    
  }
}
